# Placeholder — replace with your real UART capture

Paste the full serial log from power-on until a shell/login prompt appears.

Capture tip (macOS):

```bash
screen -L -Logfile power-on-to-shell.raw.txt /dev/cu.usbserial-XXXX 1500000
```

Then power the board with the flashed microSD inserted.
